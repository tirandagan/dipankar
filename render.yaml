services:
  - type: web
    name: nodejs-sqlite-app
    env: node
    rootDir: .
    buildCommand: npm run render-build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production